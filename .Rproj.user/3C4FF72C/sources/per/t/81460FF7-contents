top_genre <- function(movie_unlisted,genre_maxmean) {
  genre_top <- movie_unlisted[which(movie_unlisted$genres == genre_maxmean$genres[1]),]
  return(genre_top)
}
# pacman::p_load(tidyverse, targets)
# tar_load(movie_unlisted)
# tar_load(genre_maxmean)
# top_genre(movie_unlisted,genre_maxmean)
